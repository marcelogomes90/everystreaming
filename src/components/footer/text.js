import styled from "styled-components";
import { FontColor1 } from "../../style/colors";

const Text = styled.p`
    font-size: 0.75rem;
    color: ${FontColor1};
    margin: 0;
`

export default Text;
