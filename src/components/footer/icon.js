import styled from "styled-components";
import { FontColor1, SecondaryColor } from "../../style/colors";

const Icon = styled.div`
    color: ${FontColor1};

    &:hover{
        color: ${SecondaryColor};
    }
`

export default Icon;