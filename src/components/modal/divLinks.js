import styled from "styled-components";

const DivLinks = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 1rem;
    margin-top: -10px;
    opacity: 1;
`

export default DivLinks;